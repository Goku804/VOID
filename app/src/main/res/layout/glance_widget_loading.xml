<?xml version="1.0" encoding="utf-8"?>
<!--
    Placeholder only — AppWidgetProviderInfo requires SOME initialLayout
    resource to exist, but Glance replaces this with real composed content
    the moment the widget's Worker runs its first update. The user should
    essentially never see this frame.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0D0D0D" />
